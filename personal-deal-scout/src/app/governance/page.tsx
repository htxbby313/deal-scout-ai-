import { createRetentionPolicyAction, decideRetentionReviewAction } from "@/app/governance-actions";
import { WorkspaceShell } from "@/app/workspace-shell";
import { requireOwner } from "@/lib/auth";
import { getPrisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
const field = "rounded-lg border px-3 py-2 text-sm";
export default async function GovernancePage() { await requireOwner(); const [policies, reviews] = await Promise.all([getPrisma().dataRetentionPolicy.findMany({ orderBy: { createdAt: "desc" } }), getPrisma().retentionReviewRecord.findMany({ include: { policy: true }, orderBy: { dueAt: "asc" } })]); return <WorkspaceShell active="governance"><div className="mx-auto max-w-[1400px] px-4 py-6 sm:px-6"><header className="border-b pb-6"><p className="text-sm font-semibold text-blue-700">Owner-controlled data governance</p><h1 className="mt-1 text-3xl font-bold">Retention review</h1><p className="mt-2 text-sm text-slate-600">Policies begin inactive. Eligibility never deletes data automatically.</p></header><section className="mt-6 rounded-2xl border bg-white p-5"><h2 className="font-bold">Create inactive policy</h2><form action={createRetentionPolicyAction} className="mt-3 grid gap-3 sm:grid-cols-4"><select className={field} name="category">{["CONTACT_IDENTIFIER","CONSENT_EVIDENCE","CONTRACT_ARTIFACT","SETTLEMENT_ARTIFACT","AUDIT_EVENT","RESEARCH_EVIDENCE"].map((value) => <option key={value}>{value}</option>)}</select><input className={field} maxLength={2} name="jurisdictionState" placeholder="State" required /><input className={field} min="1" name="retentionDays" placeholder="Retention days" type="number" required /><button className="rounded-lg bg-slate-950 px-4 py-2 text-white">Create inactive</button></form></section><section className="mt-6 rounded-2xl border bg-white p-5"><h2 className="font-bold">Policies · {policies.length}</h2><div className="mt-3 divide-y">{policies.map((policy) => <div className="py-3" key={policy.id}><b>{policy.category} · {policy.jurisdictionState}</b><p className="text-xs text-slate-500">v{policy.version} · {policy.retentionDays} days · {policy.active ? "ACTIVE" : "INACTIVE — counsel and owner approval required"}</p></div>)}</div></section><section className="mt-6 rounded-2xl border bg-white p-5"><h2 className="font-bold">Eligibility reviews · {reviews.length}</h2><div className="mt-3 divide-y">{reviews.map((review) => <form action={decideRetentionReviewAction} className="grid gap-2 py-4 sm:grid-cols-[1fr_180px_2fr_auto]" key={review.id}><input name="reviewId" type="hidden" value={review.id} /><div><b>{review.recordType}</b><p className="text-xs text-slate-500">{review.policy.category} · due {review.dueAt.toLocaleDateString()}</p></div><select className={field} name="status" defaultValue="RETAIN"><option>RETAIN</option><option>LEGAL_HOLD</option><option>ELIGIBLE_FOR_MANUAL_DELETION</option></select><input className={field} name="reason" placeholder="Owner decision reason" required /><button className="rounded-lg bg-blue-700 px-4 py-2 text-white">Record decision</button></form>)}{!reviews.length ? <p className="text-sm text-slate-500">No records are eligible for review. No data has been deleted.</p> : null}</div></section></div></WorkspaceShell>; }
