import { describe,expect,it } from "vitest";
import { buildActivityCompleteness,buildProfitSegments,buildTimingKpis } from "./operational-kpi-extensions";
describe("operational KPI completeness",()=>{
  it("reports required milestone and per-stage timing",()=>{const result=buildTimingKpis([{createdAt:"2026-08-01",stageHistory:[{toStage:"SELLER_ENGAGED",occurredAt:"2026-08-02"},{toStage:"CONTRACTED",occurredAt:"2026-08-05"},{toStage:"CLOSED",occurredAt:"2026-08-10"}]}]);expect(result.discovery_to_contract).toEqual({average:96,median:96,sampleSize:1});expect(result.contract_to_close).toEqual({average:120,median:120,sampleSize:1});expect(result.byStage.SELLER_ENGAGED).toEqual({average:72,median:72,sampleSize:1});});
  it("segments projected and actual profit without blending them",()=>{const result=buildProfitSegments([{market:"Dallas, TX",zip:"75201",projectedCents:BigInt(100),realizedCents:BigInt(80)},{market:"Dallas, TX",zip:"75202",projectedCents:BigInt(50)}]);expect(result.market).toEqual([{key:"Dallas, TX",projectedCents:"150",realizedCents:"80",dealCount:2,realizedCount:1}]);expect(result.propertyType[0].key).toBe("UNSPECIFIED");});
  it("counts prices, packages, and terminal pipeline outcomes",()=>{expect(buildActivityCompleteness({funnels:[{stage:"NURTURE"},{stage:"ARCHIVED"},{stage:"DISQUALIFIED"}],transactions:[{controlStatus:"STOPPED"}],outcomes:[{closed:false,reason:"TITLE_DEFECT"}],buyerPrices:[{status:"INDICATIVE"},{status:"DOCUMENTED"}],dispositionPackages:[{approved:true}]})).toMatchObject({buyerPricesRequested:2,buyerPricesReceived:1,dispositionPackagesApproved:1,dealsLost:1,dealsBlocked:1,dealsStopped:1,dealsArchived:1,dealsNurtured:1});});
});
