import "server-only";

import { Prisma, type ContractPacketStructure } from "@prisma/client";
import { appendAuditEvent } from "@/lib/transaction-control";
import { getPrisma } from "@/lib/prisma";

export type ContractPacketInput = {
  transactionId: string; structure: ContractPacketStructure; closingDate: Date;
  effectiveDate?: Date; feasibilityDays: number; accessNoticeHours: number;
  earnestMoneyCents: bigint; feasibilityFeeCents: bigint; purchasePriceCents: bigint;
  assignmentFeeCents?: bigint; assignmentDepositCents?: bigint; assigneeDiligenceDeadline?: Date;
  proofOfFundsDueAt?: Date; sellerLegalName: string; buyerLegalName: string; assigneeLegalName?: string;
  legalDescription: string; propertyType: string; costAllocation: string;
  sellerNoticeInformation: string; buyerNoticeInformation: string; brokerDisclosure: string;
  assignmentContingency?: string; titleCompanyId?: string; generatedBy: string;
};

const dollars = (cents?: bigint) => cents == null ? "[NOT APPLICABLE]" : new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(Number(cents) / 100);
const day = (value?: Date) => value ? new Intl.DateTimeFormat("en-US", { dateStyle: "long", timeStyle: "short" }).format(value) : "[TO BE COMPLETED]";

export function renderTexasPacket(input: ContractPacketInput, property: { address: string; city: string; state: string; zipCode: string }, titleCompanyName?: string) {
  const title = titleCompanyName || "[TITLE COMPANY OR CLOSING AGENT]";
  const location = `${property.address}, ${property.city}, ${property.state} ${property.zipCode}`;
  const purchaseAgreement = `TEXAS REAL ESTATE PURCHASE AND SALE AGREEMENT\nATTORNEY REVIEW DRAFT\n\nThis Agreement becomes effective ${day(input.effectiveDate)} between ${input.sellerLegalName} (Seller) and ${input.buyerLegalName} and/or assigns (Buyer). Buyer acts as a principal for its own account, not as Seller's broker, agent, fiduciary, appraiser, attorney, or financial adviser.\n\n1. PROPERTY. ${location}. Legal description: ${input.legalDescription}. Property type: ${input.propertyType}. The legal description accepted by the title company controls.\n\n2. PRICE AND DEPOSITS. Purchase price: ${dollars(input.purchasePriceCents)}. Within 3 business days after the Effective Date, Buyer will deposit ${dollars(input.earnestMoneyCents)} with ${title}. Feasibility fee: ${dollars(input.feasibilityFeeCents)}.\n\n3. FEASIBILITY. Buyer has ${input.feasibilityDays} calendar days after the Effective Date to investigate condition, repairs, environmental matters, title, survey, zoning, utilities, access, leases, occupancy, financing, resale potential, marketability, and any other relevant matter. Buyer may terminate for any reason by timely written notice; earnest money will be returned.\n\n4. ACCESS. Seller will provide Buyer and Buyer's inspectors, contractors, lenders, funding sources, affiliates, prospective assignees, and prospective purchasers reasonable access on at least ${input.accessNoticeHours} hours' notice. Buyer is responsible for material physical damage directly caused by inspection, excluding pre-existing conditions and Seller negligence or misconduct.\n\n5. TITLE; CONDITION; DISCLOSURES. Seller will convey good and marketable title through ${title}, subject only to exceptions Buyer approves. Buyer may object before the later of the Feasibility Period or five business days after receipt. Buyer accepts the Property AS IS, WHERE IS, WITH ALL FAULTS, except fraud, intentional concealment, material misrepresentation, and nonwaivable disclosure duties. Seller will deliver all transaction-specific notices required by the Property and applicable law.\n\n6. EQUITABLE INTEREST DISCLOSURE. SELLER ACKNOWLEDGES THAT BUYER IS A PRINCIPAL INVESTOR, WILL HOLD ONLY A CONTRACTUAL OR EQUITABLE INTEREST BEFORE CLOSING, AND MAY EARN A PROFIT BY ASSIGNMENT, NOMINATION, TRANSACTIONAL FUNDING, OR A SEPARATE RESALE CLOSING. BUYER DOES NOT HOLD LEGAL TITLE BEFORE CLOSING AND IS NOT SELLER'S BROKER OR ADVISER. Seller initials: ________.\n\n7. ASSIGNMENT AND CLOSING ALTERNATIVES. Buyer may assign this Agreement in whole or part without additional Seller consent, nominate an affiliate, funding source, partner, or third-party purchaser, close in Buyer's name, or elect a separate resale after acquiring title. No assignment releases Buyer unless Seller signs a written release or novation. Before Closing, Buyer may market only its contractual or equitable interest and may not advertise ownership of Seller's legal title.\n\n8. SELLER REPRESENTATIONS. Seller represents actual authority to convey, no undisclosed conflicting contract or ownership claim, disclosure of known occupants and leases, and disclosure of uncured written condemnation, code, or environmental notices. Seller will not further encumber or contract to sell while this Agreement is effective.\n\n9. CLOSING. Closing occurs on or before ${day(input.closingDate)} through ${title}. Costs: ${input.costAllocation}. Risk of loss remains with Seller until Closing.\n\n10. DEFAULT. For Buyer's ordinary failure to close after the Feasibility Period and a three-business-day written cure period, Seller's sole and exclusive monetary remedy is the earnest money as agreed liquidated damages. Exceptions: fraud, intentional misconduct, confidentiality, or inspection indemnity. If Seller defaults, Buyer may terminate and recover earnest money and feasibility fee, seek specific performance where available, or recover proven direct title, survey, inspection, and financing expenses, without double recovery.\n\n11. GENERAL. Texas law governs; venue is the Property county; time is of the essence. Amendments, waivers, and any release or novation must be signed. Electronic signatures may be used if lawful and accepted by title. Broker disclosure: ${input.brokerDisclosure}. Seller notices: ${input.sellerNoticeInformation}. Buyer notices: ${input.buyerNoticeInformation}.\n\nThis template is not an attorney-approved guarantee. Texas counsel and the closing title company must review the completed packet and determine all transaction-specific notices and addenda.\n\nSELLER: __________________ Date: ________    BUYER: __________________ Date: ________`;

  const disclosure = `TEXAS EQUITABLE INTEREST DISCLOSURE\n\nProperty: ${location}\nSeller: ${input.sellerLegalName}\nBuyer/Assignor: ${input.buyerLegalName}\nPotential Assignee: ${input.assigneeLegalName || "[ASSIGNEE LEGAL NAME]"}\n\nSELLER ACKNOWLEDGMENT. Before entering the purchase contract, Seller acknowledges that Buyer intends to assign or sell Buyer's contractual or equitable interest for compensation; Buyer does not hold legal title before closing and acts as a principal, not Seller's broker, agent, fiduciary, appraiser, attorney, or financial adviser. Seller initials: ________.\n\nASSIGNEE ACKNOWLEDGMENT. Before entering an assignment, Assignee acknowledges that Assignor is assigning only a contractual or equitable interest, does not hold legal title, is not conveying the real property as record owner, and may earn a profit included in the assignment fee. Assignee initials: ________.\n\nThe parties intend these separate written acknowledgments to support Texas Property Code Section 5.0205. This form is not an attorney-approved guarantee and does not replace transaction-specific notices or legal/title review.\n\nSELLER: __________________ Date: ________\nBUYER/ASSIGNOR: __________________ Date: ________\nASSIGNEE: __________________ Date: ________`;

  const assignmentAgreement = input.structure === "ASSIGNMENT" ? `TEXAS ASSIGNMENT OF REAL ESTATE PURCHASE CONTRACT\nATTORNEY REVIEW DRAFT\n\nEffective ${day(input.effectiveDate)}, ${input.buyerLegalName} (Assignor) assigns to ${input.assigneeLegalName || "[ASSIGNEE LEGAL NAME]"} (Assignee) its buyer interest in the contract for ${location}. Assignor is a principal and is assigning only a contractual or equitable interest, not legal title.\n\n1. ASSUMPTION. Assignee accepts the assignment and assumes buyer obligations arising on and after this Assignment, including funding, deposits, diligence, indemnities, and Closing. Assignor is not automatically released from the original contract; Assignee indemnifies Assignor for Assignee's failure to perform assumed obligations.\n\n2. FEE AND DEPOSIT. Assignment fee: ${dollars(input.assignmentFeeCents)}. Assignment deposit: ${dollars(input.assignmentDepositCents)}, due to ${title}. At Closing it is credited against the fee; the balance is a separate settlement-statement item paid through settlement.\n\n3. DILIGENCE AND FUNDING. Assignee's diligence deadline is ${day(input.assigneeDiligenceDeadline)}. Before that deadline Assignee may terminate and receive the deposit. Afterward, refund exceptions include Seller or Assignor default, an uncured title defect preventing Closing, qualifying casualty, or the closing agent's determination that it cannot lawfully close despite reasonable cooperation. Proof of immediately available funds or committed financing is due ${day(input.proofOfFundsDueAt)}. Contingency: ${input.assignmentContingency || "NONE"}.\n\n4. CLOSING. Closing is due ${day(input.closingDate)}. No further assignment is permitted without Assignor's prior written consent. Assignee may not record this Assignment or any memorandum, cloud title, bypass Assignor, interfere with the underlying contract, or use confidential information to avoid the fee.\n\n5. MATERIAL CLOSING DEFAULT. If, after diligence, Assignee materially defaults by failing to fund or close, repudiating without contractual basis, or causing termination of the underlying contract, and fails to cure within two business days after notice, Assignor may terminate and receive the assignment deposit as a reasonable estimate of difficult-to-measure transaction loss and as liquidated damages, not a penalty. Retention is Assignor's sole and exclusive monetary remedy for ordinary failure to close. Separate remedies remain for fraud, intentional misconduct, unauthorized recording or clouding of title, indemnity obligations, confidentiality breach, intentional circumvention, or interference with Assignor's rights. No double recovery is permitted.\n\n6. LIMITED REPRESENTATIONS. Assignor represents only that it entered the original contract, has not previously assigned the same interest, attached known amendments, and has authority to sign. Assignor makes no independent warranty of title, value, condition, repairs, square footage, zoning, rents, taxes, liens, environmental matters, occupancy, financing, or profit; fraud and intentional concealment are not waived.\n\n7. GENERAL. Texas law governs, venue is the Property county, and time is of the essence. The original contract is Exhibit A. This template is not an attorney-approved guarantee; Texas counsel and the closing title company must review the completed transaction.\n\nASSIGNOR: __________________ Date: ________    ASSIGNEE: __________________ Date: ________` : undefined;
  return { purchaseAgreement, assignmentAgreement, equitableInterestDisclosure: disclosure };
}

export async function createContractPacket(input: ContractPacketInput) {
  const db = getPrisma();
  return db.$transaction(async (tx) => {
    const transaction = await tx.dealTransaction.findUnique({ where: { id: input.transactionId }, include: { property: true } });
    if (!transaction) throw new Error("Transaction not found.");
    if (transaction.jurisdictionState !== "TX") throw new Error("These templates are limited to Texas property transactions.");
    if (transaction.controlStatus === "STOPPED") throw new Error("A stopped transaction cannot generate a packet.");
    const titleCompany = input.titleCompanyId ? await tx.titleCompany.findUnique({ where: { id: input.titleCompanyId } }) : null;
    const content = renderTexasPacket(input, transaction.property, titleCompany?.name);
    const latest = await tx.contractPacket.findFirst({ where: { transactionId: input.transactionId }, orderBy: { version: "desc" }, select: { version: true } });
    const packet = await tx.contractPacket.create({ data: { ...input, version: (latest?.version ?? 0) + 1, ...content } });
    await Promise.all([
      tx.transactionDocument.create({ data: { transactionId: input.transactionId, type: "TEXAS_CONTRACT_PACKET", title: `Texas ${input.structure.replaceAll("_", " ")} packet`, version: packet.version, storageKey: `database:contract-packet:${packet.id}`, status: "DRAFT" } }),
      tx.dealTransaction.update({ where: { id: input.transactionId }, data: { transactionStructure: input.structure } }),
    ]);
    await appendAuditEvent(tx, input.transactionId, "contract.packet.generated", input.generatedBy, `Generated Texas ${input.structure.replaceAll("_", " ").toLowerCase()} packet version ${packet.version}.`, { packetId: packet.id, titleCompanyId: input.titleCompanyId ?? null } as Prisma.InputJsonValue);
    return packet;
  }, { isolationLevel: Prisma.TransactionIsolationLevel.Serializable });
}
