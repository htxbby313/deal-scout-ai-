import{describe,expect,it}from"vitest";import{realizedProfitBuckets,reconcileFinancials,validateExpenseLines}from"./financial-reconciliation";describe("financial reconciliation",()=>{const lines=[{category:"TITLE",amountCents:BigInt(100),sourceReference:"line-1"},{category:"LEGAL",amountCents:BigInt(50),sourceReference:"line-2"}];it("requires itemized expenses to equal reviewed total",()=>{expect(validateExpenseLines(lines,BigInt(150)).valid).toBe(true);expect(validateExpenseLines(lines,BigInt(151)).blockers).toContain("expense_lines_do_not_reconcile");});it("reports projected to actual variance",()=>expect(reconcileFinancials({projectedNetCents:BigInt(1000),actualGrossCents:BigInt(1200),actualExpenses:lines}).varianceCents).toBe(BigInt(50)));it("separates monthly quarterly annual and lifetime",()=>expect(realizedProfitBuckets([{reviewedAt:new Date("2026-08-01"),realizedProfitCents:BigInt(100)},{reviewedAt:new Date("2025-01-01"),realizedProfitCents:BigInt(50)}],new Date("2026-08-19"))).toEqual({monthlyCents:"100",quarterlyCents:"100",annualCents:"100",lifetimeCents:"150"}));});
