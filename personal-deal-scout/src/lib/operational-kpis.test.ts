import { describe,expect,it } from "vitest";import { buildOperationalKpis } from "@/lib/operational-kpis";
describe("operational KPI definitions",()=>{it("keeps financial truth classes separate and exposes counts",()=>{const now=new Date("2026-08-19");const report=buildOperationalKpis({properties:[{createdAt:now,researched:true,researchException:false}],funnels:[],engagements:[],transactions:[],outcomes:[],buyerEvidence:[],costs:[],profits:{projectedCents:BigInt(100),weightedCents:BigInt(50),contractedCents:BigInt(20),realizedCents:BigInt(10),realizedValues:[BigInt(10)]},evidence:[],approvals:[],agentTasks:[],windowStart:now,windowEnd:now,refreshedAt:now});expect(report.metrics.find(m=>m.key==="projected_pipeline")?.value).toBe("100");expect(report.metrics.find(m=>m.key==="realized_profit")?.value).toBe("10");expect(report.metrics.every(m=>"numerator" in m&&"denominator" in m)).toBe(true);});it("warns on misleadingly small percentage samples",()=>{const now=new Date();const report=buildOperationalKpis({properties:[{createdAt:now,researched:true,researchException:false}],funnels:[],engagements:[],transactions:[],outcomes:[],buyerEvidence:[],costs:[],profits:{projectedCents:BigInt(0),weightedCents:BigInt(0),contractedCents:BigInt(0),realizedCents:BigInt(0),realizedValues:[]},evidence:[],approvals:[],agentTasks:[],windowStart:now,windowEnd:now,refreshedAt:now});expect(report.metrics.find(m=>m.key==="research_completion_rate")?.warning).toContain("Small sample");});});
